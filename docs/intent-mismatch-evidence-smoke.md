# Intent-mismatch evidence smoke test

This documentation-only change intentionally does **not** implement the JSON
export requested by linked issue #16. It describes the Auto-Merge experiment
instead so the Review Agent and Auto-Merge semantic stage can test whether a
pull request is actually aligned with its linked issue.
