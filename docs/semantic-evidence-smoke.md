# Semantic evidence smoke test

This documentation-only change tests that Auto-Merge receives the requested
intent, changed-file context, Review Agent rationale, and optional trace
references as one bounded evidence package.
